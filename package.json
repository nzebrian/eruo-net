{
  "name": "eruo-net",
  "version": "0.1.0",
  "private": true,
  "description": "EruoNet Cloud SaaS monorepo foundation.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate-repo.mjs",
    "lint": "node scripts/validate-repo.mjs --lint",
    "format:check": "node scripts/validate-repo.mjs --format-check",
    "security:check": "node scripts/validate-repo.mjs --security-check",
    "ci": "npm test && npm run validate && npm run lint && npm run format:check && npm run security:check"
  },
  "engines": {
    "node": ">=20"
  }
}
