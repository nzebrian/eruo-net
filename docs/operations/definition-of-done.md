# Definition of Done

A Milestone 1 change is done when:

- It preserves the locked product baseline.
- It does not implement deferred product functionality.
- It keeps application and package boundaries clear.
- `npm run ci` passes.
- Docker Compose configuration validates when Docker is available.
- Security validation finds no committed secrets or risky placeholders.
- Documentation is updated for architectural or operational decisions.
